services:
  - type: web
    name: snapfix-api
    env: python
    region: oregon
    buildCommand: ""
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
    plan: free
    runtime: python
